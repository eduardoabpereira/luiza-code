// MongoDB Playground
// Use Ctrl+Space inside a snippet or a string literal to trigger completions.

// The current database to use.
use('luizaCode');

// Search for documents in the current collection.
db.getCollection('wishlist')
  .find({})
  .sort({
    _id: -1
  });
